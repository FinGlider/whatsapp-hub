# Server Configuration
PORT=3138
NODE_ENV=development

# MySQL Database Configuration
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=your_username
DB_PASSWORD=your_password
DB_NAME=whatsapp_hub

# Redis Configuration (for Bull queue)
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=

# WhatsApp API Configuration (Global)
WHATSAPP_API_URL=https://graph.facebook.com/v21.0

# Note: Access tokens and verify tokens are now stored per app in the database
# Each Meta app has its own tokens stored in the 'apps' table
# Use the Admin API to manage them:
#   POST /admin/apps - Create app with: { id, business_id, name, verify_token, access_token }
#   GET /admin/business-accounts/:id/apps - View all apps with their tokens
